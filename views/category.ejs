<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= restaurantName %> - <%= category.charAt(0).toUpperCase() + category.slice(1) %></title>
</head>
<body>
    <%- include('./partials/nav') %>
  <h1><%= restaurantName %> - <%= category.charAt(0).toUpperCase() + category.slice(1) %></h1>

    <ul>
      <% menuItems.forEach((item) => { %>
        <li>
          <h2><%= item.name %></h2>
          <p><%= item.details %></p>
          <p class="price">Price: $<%= item.price.toFixed(2) %></p>
          <p>Rating: <%= item.rating %>/5 <%= item.rating === 5 ? '⭐' : '' %></p>
          
        </li>
      <% }); %>
    </ul>
</body>
</html>
